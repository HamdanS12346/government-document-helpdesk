import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Government Helpdesk | Digital India",
  description:
    "Ask about government documents, procedures, eligibility, fees and more. Powered by AI.",
  keywords: "government, helpdesk, documents, India, Digital India, citizen services",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
